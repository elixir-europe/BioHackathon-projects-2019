import React from 'react';
import {QuackNavBar} from '../components/QuackNavBar';

const QuackResults = () => {
    return (
        <div>
            <QuackNavBar/>
        </div>
    );
};

export default QuackResults;